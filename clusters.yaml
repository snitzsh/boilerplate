regions:
  north-america:
    clusters:
      # dev, sit, uat, prod
      dev:
        active: true
        account: null # if null it will create the cluester in the global account
        cloud: "aws"
        helm-charts: []
      sit: {}
      uat: {}
      prod: {}
  south-america: {}
  europe: {}
  africa: {}
  asia: {}
  australia: {}

  # uat:
  #   active: false
  #   account: null # if null it will create the cluester in the global account
  #   cloud: "aws"
  # prod:
  #   active: false
  #   account: null # if null it will create the cluester in the global account
  #   cloud: "aws"

  # - name: "dev"
  #   active: true
  #   cloud: "aws" # aws, azure, gcp (google cloud platform)
  # - name: "black"
  #   active: true
  #   cloud: "aws" # aws, azure, gcp (google cloud platform)
